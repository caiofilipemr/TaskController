<p id="notice"><%= notice %></p>
<div style="margin: 0 18px 0">
  <p>
    <strong>Title:</strong>
    <%= @task.title %>
  </p>

  <p>
    <strong>Details:</strong>
    <%= @task.details %>
  </p>

  <%= link_to 'Edit', edit_task_path(@task), class: 'btn btn-primary col-md-1', style: 'margin: 2px' %>
  <%= link_to 'Back', tasks_path, class: 'btn btn-default col-md-1', style: 'margin: 2px' %>
</div>
